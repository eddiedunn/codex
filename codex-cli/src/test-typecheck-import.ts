import type { ResponseItem } from "./utils/responses.js";

// If this line fails, it means TypeScript is not resolving .js extension imports to .ts files correctly in typecheck config.
const _dummy: ResponseItem | undefined = undefined;
