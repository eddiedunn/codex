// This file is the CLI entrypoint for TemplatesList. It should only import and render the component, not use it as a type.
import React from 'react';
import { render } from 'ink';
import TemplatesList from './templates.js';

render(<TemplatesList />);
